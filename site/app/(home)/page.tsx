import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Neovim Mine",
  description: "Mine of neovim plugins and etc.",
};

export default async function Home() {
  return <></>;
}
